function renderSingle(sd, stateName, yrs, activeYear) {
  console.log('single render', sd, stateName, yrs, activeYear);
  
  // data.forEach((d) => {
  //   d.YEAR = parseInt(d.YEAR);
  //   d.AVG_SCORE = parseInt(d.AVG_SCORE);
  //   d.GDP = parseInt(d.GDP);
  //   d.GDP_PER_CAPITA = parseFloat(d.GDP_PER_CAPITA);
  //   d.INSTRUCTION_EXPENDITURE = parseInt(d.INSTRUCTION_EXPENDITURE);
  // });

  // const yearData = data.filter(function(point) { return point.YEAR === activeYear})
  // let state1 = yearData.filter(function(point) {return point.STATE === stateName.toUpperCase();})
  
  return;
}
